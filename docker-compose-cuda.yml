version: '3.8'

services:
  llamacpp:
    image: joshxt/local-llm:cuda
    env_file: .env
    restart: unless-stopped
    ports:
      - "8091:8091"
    volumes:
      - ./models:/app/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
